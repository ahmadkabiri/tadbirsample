const endpoints = {
    account : {
        login : {
            method : 'POST',
            endpoint : 'Account/login'
        },
        profile : {
            method : "GET",
            endpoint : "user/my-profile"
        }
    }
}

export default endpoints